interface:
  display_name: "Project Graph"
  short_description: "Map repos into reusable graph context"
  default_prompt: "Use $project-graph to build or refresh a cached graph before answering architecture, impact, test, route, or tooling questions."
